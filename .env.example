# WordPress Connection Configuration
# ===================================

# WordPress site URL (your existing WordPress site)
# Example: https://your-wordpress-site.com
WORDPRESS_URL=

# WordPress authentication credentials
# Use Application Passwords instead of regular passwords for better security
# Generate at: WordPress Admin → Users → Profile → Application Passwords
WORDPRESS_USERNAME=
WORDPRESS_PASSWORD=

# Alternative JWT authentication (if JWT plugin is installed)
# Leave empty if using username/password authentication
WORDPRESS_JWT_TOKEN=



