# Test environment configuration
# Copy this to .env.test and customize for your test environment

# Test WordPress instance (if testing against real WordPress)
WORDPRESS_URL=https://test.example.com
WORDPRESS_USERNAME=testuser
WORDPRESS_PASSWORD=testpass123

# Test mode - set to true to use mock responses instead of real API calls
TEST_MODE=true

# Test database (if using database tests)
TEST_DB_URL=sqlite:///test.db